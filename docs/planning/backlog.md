# Gravity Freight V2: Development Backlog

この Backlog は未完了タスクのみを管理する。
作業主体は以下の区分で明示する。

- **Codex 作業**: 開発、修正、仕様更新、テスト追加、自己レビュー。
- **Owner 確認**: 実ゲームまたはブラウザでの確認。Owner が確認完了と明言したら削除する。
    - Owner 確認項目には、完了確認時の意識疎通を容易にするため `OC-*` の ID を付与する。
    - 複数画面、複数施設、複数操作にまたがる確認は、完了判定できる単位へ分解する。
- **完了条件不明**: そのままでは完了判定できないタスク。削除するか、完了条件を定義して移動する。

## Codex 作業

### 仕様・設計
- [ ] core_scenarios に記載されているシナリオで必要となるクラスのインターフェースを設計する
    - core_scenarios.md で進捗管理するため、Backlog には詳細の進捗は記載しない
    - 1回のセッションで設計するシナリオは1～2個程度とする
- [ ] 初版リリース前に `common_style.md` の移行期間中の対応節を削除する
    - リリース版ドキュメントには β v1 / 旧仕様の class 名を残さない
### 実装ルール・横断整備
- [ ] 実装開始前に TDD 方針とテスト命名規約を適用する
    - 複雑なロジック、状態管理、イベント連鎖を伴う実装は、実装前にテストを作成する
    - 要求仕様テストは `tests/spec/` に配置し、命名は `[prefix]_[章番号]_[名称].test.js` とする
    - 実装テストは `tests/implementation/` に `src/` と同じ構成で配置し、命名は `[元のファイル名].test.js` とする
    - 実装タスクごとに、対象が要求仕様テスト・実装テストのどちらを必要とするかを確認してから着手する
- [ ] GameController のファイルサイズと責務境界を見直し、施設処理、航行開始・予測線、航行結果処理などを適切なクラスへ分割する
- [ ] WorldRenderer のファイルサイズと責務境界を見直し、マップオブジェクト描画、航行ビジュアル連携、描画ループ制御を適切なクラスへ分割する
- [ ] `archive.css` の責務分割を行う
    - Analytic Archive 固有のレイアウト、テーブル、実績カード表示が同一ファイルに集約されているため、ファイルサイズと責務境界を見直す
- [ ] `git diff --check` 実行時の CRLF 警告を解消する
    - `.gitattributes`、既存ファイルの改行コード、Windows 環境での運用を確認し、不要な大規模差分を避けて対応する
- [ ] 依存関係の脆弱性警告を確認・解消する
    - `npm audit` で警告が出ている場合は、リリース前に影響範囲を確認する
    - `npm audit fix` または依存バージョン更新を行う場合は、lockfile 変更、build サイズ、描画挙動への影響を確認する
    - 自動修正で破壊的な依存更新が必要になる場合は、対応方針を確認してから実施する

### データ・永続化
- [ ] FlightReplaySnapshots 完成後に保存データサイズを検証する
    - 目標: Gravity Freight の保存データ総量は 1MiB 以内を目安とする
    - 目標: リプレイ関連データは 20件保存時に 600KiB 以内、1件平均 30KiB 以内を目安とする
    - 警告: リプレイ関連データが 20件で 700KiB、または 1件平均 35KiB を超える場合は圧縮や保存項目削減を検討する
    - 見直し: リプレイ関連データが 20件で 800KiB、または 1件平均 40KiB を超える場合は snapshot 構造を見直す
    - 測定方法: localStorage で保存される文字列サイズを重視し、`JSON.stringify(data).length * 2` による UTF-16 相当 byte を基準値とする。参考値として `TextEncoder` による UTF-8 byte も併記する
    - 参考: v0.79 の代表データ測定では、`RocketSnapshot` / `SectorSnapshot` を含む20件の実装テストで 600KiB 以内を確認
    - [ ] 圧縮の要否、圧縮後サイズ、復元コストを評価する
    - [ ] 共通 DataManager の保存方式で問題なく扱えるか確認する
- [ ] content/data 系リソースの i18n 対応を棚卸しして実施する
    - [ ] ストーリー本文、タイトル、発見文
    - [ ] 実績ラベル、tier title
    - [ ] How To Play テキスト
    - [ ] アイテム名、説明文、施設名など、その他表示用マスタデータ

### ゲームフロー・航行
- [ ] M4: 航行終了後の記録・経済・施設導線を接続する
    - [ ] リプレイ再生画面に `ReplayContext` を接続する
        - Analytic Archive の Replays タブで選択した記録 ID から `FlightRecorder.createReplayContext(recordId)` を呼ぶ入口は接続済み
        - 残りは `ReplayContext` を用いたマップ表示、物理再生ループ、再生終了後に Replays タブへ戻る導線の実装
- [ ] リプレイ再生を実装する
- [ ] ゲームオーバー画面・ゲームオーバー演出を完成させる
    - ゲーム終了条件成立時に、現在セクターのマップからズームアウトする逆方向ワープを開始する
    - 逆方向ワープ中にレシート形式の最終リザルトを表示する
    - `END CONTRACT` 押下後にワープを減速し、タイトルへ戻る導線を確認できるようにする
- [ ] **CX-GAME-001** HUD の現在コイン表示が、施設で何も購入しなかった後でも正しい値になるよう修正する
    - Owner 確認結果: 直っていない。施設で何も購入しなかったときにずれる可能性がある。
- [ ] **CX-GAME-002** Black Market でアイテム購入後に獲得アイテムが表示されるよう修正する
- [ ] **CX-GAME-003** Black Market の「1回の施設で購入1回限り」制限を修正する
- [ ] **CX-GAME-004** 手紙入手時に HUD のメールアイコンへ反映されるよう修正する
- [ ] **CX-GAME-005** 今回の航行でない手紙が航行結果画面に表示され続けないよう修正する
- [ ] **CX-GAME-006** 配送 cargo を分かりやすくするため、段ボールアイコン hover 時に配送ボーナス説明を表示する
- [ ] **CX-GAME-007** 配送 cargo の ItemCard 右肩に、配送先施設色の段ボールアイコンを表示する
- [ ] **CX-GAME-008** 航行結果画面で配送 cargo につく `Delivered` タグの表示とスタイルを修正する
- [ ] **CX-GAME-009** 航行結果画面で unmatched の Delivery Bonus は `+0` スコアを表示しないようにする
- [ ] **CX-GAME-010** delivery bonus のアイテムに coin が含まれる場合、その coin は `Delivery Bonus` ではなく通常の獲得コインとして表示する

### 画面別 UI 実装
- [ ] How To Play を実装する
    - [ ] HowToPlayUI
    - [ ] HowToPlayDiagrams
    - [ ] 1ページ目画像の白い帯にある「軌道の残響(ORBITAL ECHOES)」テキストを削除する
        - 画像は説明書本文で使うため、言語依存・説明過多な文字情報を画像内に持たせない
- [ ] Analytic Archive を仕上げる
    - [ ] achievement カードの表記と幅を確認・調整する
        - 分子は現在値、分母は次の tier の値を表示する
        - カード背景の tier 表記をモックに合わせる
        - カード幅をモックに合わせる
    - [ ] サイドパネルの achievement progress の計算方法を確認・調整する
        - achievement 解放率ではなく、tier 3 までの解放段階で進捗を表示する
    - [ ] サイドパネルの `MAX SECTOR` ラベルを実態に合う名称へ見直す
        - 現在表示している内容が累積クリアセクター数なら、ラベルもそれに合わせる
    - [ ] 今回ゲームで記録された ranking / replay record に `NEW` 表示を付ける
        - ranking テーブルでは、推移グラフ用に保持している最新20回のうち直近ゲームと一致する記録に `NEW` タグを表示する
        - Replays タブでは、前回ゲームで記録されたすべての航行記録に `NEW` タグを表示する
        - 航行結果画面の protect 選択テーブルでも、今回ゲームで記録された航行記録に `NEW` タグを表示する
        - ranking / replay record / 航行結果画面で同一ゲーム由来の記録を識別できる共通 ID または記録グループ ID の導入を検討する
- [ ] 実績表示を実装する
    - [ ] achievement 達成時のトースト表示を仕様化・実装する
        - Owner 確認結果: 実装されているか不明。実装有無を確認し、未実装なら実装する。
    - [ ] achievement 獲得時演出を確認・実装する
        - Owner 確認結果: 実装されているか不明。実装有無を確認し、未実装なら実装する。
- [ ] 設定画面を実装する
    - 音量設定、カメラリセット、言語切り替えの表示・操作・保存/反映範囲を確認できるようにする
    - Owner 確認結果: 音量設定、カメラリセット、言語切り替えが実装されているか不明。実装有無を確認し、未実装なら実装する。
- [ ] ストーリーモーダルを実装する
    - 既読更新、施設別表示、本文表示、閉じる導線を確認できるようにする
- [ ] ゲーム中から設定画面を開く導線を検討・実装する
    - 音量設定、カメラリセット、言語設定をビルドパネル周辺から操作できるか確認する
## Owner 確認

### 実ゲーム確認
- [ ] **OC-GAME-003** Repair Dock の修理・メンテナンス時、施設画面下部の所持コイン表示がカウントダウンすることを確認する
- [ ] **OC-GAME-004** Black Market の購入時、施設画面下部の所持コイン表示がカウントダウンすることを確認する
- [ ] **OC-GAME-026** Black Market 画面のモックとの差分、取引フロー、アイテムカード、ボタン状態、文言をレビューする

### UI 表示確認
- [ ] **OC-UI-001** 航行結果画面の回収アイテムリストが、アイテム単位で時間差表示されることを確認する
- [ ] **OC-UI-002** Repair Dock の受け取りパーツリストが、アイテム単位で時間差表示されることを確認する
- [ ] **OC-UI-003** Black Market の受け取りアイテムリストが、アイテム単位で時間差表示されることを確認する
- [ ] **OC-UI-004** ボタン操作やアイテム選択時に SE が聞こえることを確認する
